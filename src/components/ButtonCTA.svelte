<button>Contact</button>

<style>
        button {
                letter-spacing: 0.6px;
                font-family: var(--Raleway);
                background-image: linear-gradient(to left, var(--CTA), orange);
                color: white;
                font-size: 1rem;
                font-weight: 600;
                max-width: 100px;
                padding: 20px 20px;
                z-index: 2;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.497);
                transition: transform 0.5s;
                text-transform: lowercase;
             
        }
        button:hover {
                transform: scale(1.1);
        }
        button:active {
                border: 2px white solid;
        }
  
</style>
